import styles from './FlexiblePaymentSection.module.scss';

const FlexiblePaymentSection = () => (
  <section className={styles.container}>
    <h2>Flexible payment section</h2>
  </section>
);

export default FlexiblePaymentSection;
